diff --git a/dist/client/route-loader.js b/dist/client/route-loader.js
index 888fc23e628cd87fbbf60742aeaaea37b9703f6e..259e82f95ee33fee4614010e4cc2c8cafd56d280 100644
--- a/dist/client/route-loader.js
+++ b/dist/client/route-loader.js
@@ -124,6 +124,12 @@ function appendScript(src, script) {
         // 3. Finally, set the source and inject into the DOM in case the child
         //    must be appended for fetching to start.
         script.src = src;
+
+        // Set integrity if we have a matching hash
+        const hashManifest = window.__CHUNK_SRI_MANIFEST || {};
+        if (hashManifest[src]) {
+            script.integrity = hashManifest[src];
+        }
         document.body.appendChild(script);
     });
 }
diff --git a/dist/esm/client/route-loader.js b/dist/esm/client/route-loader.js
index 42ec95bb5246fc94b268479f69def4ab3b101199..8da1cad0a56eebfd0c2b9ce9902d8284a8d32cd0 100644
--- a/dist/esm/client/route-loader.js
+++ b/dist/esm/client/route-loader.js
@@ -94,6 +94,12 @@ function appendScript(src, script) {
         // 3. Finally, set the source and inject into the DOM in case the child
         //    must be appended for fetching to start.
         script.src = src;
+
+        // Set integrity if we have a matching hash
+        const hashManifest = window.__CHUNK_SRI_MANIFEST || {};
+        if (hashManifest[src]) {
+            script.integrity = hashManifest[src];
+        }
         document.body.appendChild(script);
     });
 }
